import DashboardConfigWork from "@/ui/dashboard-config-work";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "ichiyo | 作品",
};

export default function PageDashboardConfigWork() {
  return <DashboardConfigWork />;
}
